# FreeCode Examples

This directory contains example code generated by FreeCode to demonstrate its capabilities.

## JavaScript Examples

### Array Sorting
```javascript
function sortArray(arr) {
    return arr.slice().sort((a, b) => a - b);
}

const numbers = [64, 34, 25, 12, 22, 11, 90];
const sorted = sortArray(numbers);
console.log(sorted); // [11, 12, 22, 25, 34, 64, 90]
```

### Fibonacci Calculator
```javascript
function fibonacci(n) {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

// More efficient version with memoization:
function fibonacciMemo(n, memo = {}) {
    if (n in memo) return memo[n];
    if (n <= 1) return n;
    memo[n] = fibonacciMemo(n - 1, memo) + fibonacciMemo(n - 2, memo);
    return memo[n];
}

console.log(fibonacci(10)); // 55
```

### String Reversal
```javascript
function reverseString(str) {
    return str.split('').reverse().join('');
}

// Alternative implementation
function reverseStringLoop(str) {
    let reversed = '';
    for (let i = str.length - 1; i >= 0; i--) {
        reversed += str[i];
    }
    return reversed;
}

console.log(reverseString("hello")); // "olleh"
```

## Python Examples

### Array Sorting
```python
def sort_array(arr):
    """Sort an array of numbers in ascending order."""
    return sorted(arr)

numbers = [64, 34, 25, 12, 22, 11, 90]
sorted_numbers = sort_array(numbers)
print(sorted_numbers)  # [11, 12, 22, 25, 34, 64, 90]
```

### Fibonacci Calculator
```python
def fibonacci(n):
    """Calculate the nth Fibonacci number."""
    if n <= 1:
        return n
    return fibonacci(n - 1) + fibonacci(n - 2)

# More efficient version with memoization:
def fibonacci_memo(n, memo={}):
    if n in memo:
        return memo[n]
    if n <= 1:
        return n
    memo[n] = fibonacci_memo(n - 1, memo) + fibonacci_memo(n - 2, memo)
    return memo[n]

print(fibonacci(10))  # 55
```

### String Reversal
```python
def reverse_string(s):
    """Reverse a string."""
    return s[::-1]

# Alternative implementation
def reverse_string_loop(s):
    reversed_str = ""
    for char in s:
        reversed_str = char + reversed_str
    return reversed_str

print(reverse_string("hello"))  # "olleh"
```

## Java Examples

### Array Sorting
```java
import java.util.Arrays;

public class ArraySorter {
    public static int[] sortArray(int[] arr) {
        int[] copy = arr.clone();
        Arrays.sort(copy);
        return copy;
    }
    
    public static void main(String[] args) {
        int[] numbers = {64, 34, 25, 12, 22, 11, 90};
        int[] sorted = sortArray(numbers);
        System.out.println(Arrays.toString(sorted));
        // Output: [11, 12, 22, 25, 34, 64, 90]
    }
}
```

### Fibonacci Calculator
```java
public class Fibonacci {
    public static int fibonacci(int n) {
        if (n <= 1) return n;
        return fibonacci(n - 1) + fibonacci(n - 2);
    }
    
    // More efficient version with dynamic programming
    public static int fibonacciDP(int n) {
        if (n <= 1) return n;
        
        int[] dp = new int[n + 1];
        dp[0] = 0;
        dp[1] = 1;
        
        for (int i = 2; i <= n; i++) {
            dp[i] = dp[i - 1] + dp[i - 2];
        }
        
        return dp[n];
    }
    
    public static void main(String[] args) {
        System.out.println(fibonacci(10)); // 55
    }
}
```

## Usage Tips

1. **Be Specific**: The more specific your prompt, the better the generated code
2. **Include Context**: Mention the programming language and any specific requirements
3. **Ask for Examples**: Request usage examples to understand how the code works
4. **Request Explanations**: Ask for explanations to learn from the generated code

## Contributing Examples

Have a great example generated by FreeCode? Consider contributing it to this collection!

1. Fork the repository
2. Add your example to this file
3. Include both the prompt and the generated code
4. Submit a pull request

## More Examples

Visit the [FreeCode repository](https://github.com/teem02/FreeCode) for more examples and to try the platform yourself!